

                genericità    autotest     void safe
linked_list         NO            SI           NO
VS_linked_list      NO            SI           SI
gen_linked_list     SI            SI           NO
my_linked_list      SI            NO           SI (di Luca Biondo)

gen_vs_linked_list deve aggiungere a my_linked list gli autotest

(VS_ e gen_ sono le più "vecchie" in termini di numero di feature e organizzazione)

linked_list (ha commenti in italiano)
  è un po' più indietro di my_linked_list (commenti in inglese) che ha implementato feature che erano solo in bozza
  è un po' più avanti di my_linked_list perché ha il nuovo design delle liste con active_element

c'è in LINKED_LIST e non in MY_LINKED_LIST
    - sum_of_positive (Perché non ha senso per versione generica)
    - active_element (non ha senso in MY_LINKED_LIST perché relativa al nuovo design della lista con ACTIVE_ELEMENT)
    - remove_active (non ha senso in MY_LINKED_LIST perché relativa al nuovo design della lista con ACTIVE_ELEMENT)
    - remove_first (non ha senso in MY_LINKED_LIST perché relativa al nuovo design della lista con ACTIVE_ELEMENT)
    - remove_last (non ha senso in MY_LINKED_LIST perché relativa al nuovo design della lista con ACTIVE_ELEMENT)

c'è in MY_LINKED_LIST e non in LINKED_LIST
 - attached_element (probabilmente non serve)
 - remove (va inserita ma con paramentro l'elemento e non il valore )
 - implementazione di
   - insert_after_using_has_and_get_element
   - insert_before_using_has
   - value_after
   - value_precedes
   - value_before

DA_FARE
 - allineare VS_linked_list in termini di feature usando contemporaneamente linked_list (che è non VS) e my_linked_list (che è VS)

 - ristrutturare i test con le classi di test una per feature che contiene la costruzione delle istanze e l'invocazione dei test

